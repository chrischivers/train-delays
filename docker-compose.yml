version: '2'
services:

  postgres:
      image: postgres:9.6.2
      ports:
          - 5432:5432
      environment:
        POSTGRES_DB: traindelays
  redis:
    image: redis:3.2-alpine
    command: ["redis-server"]
    hostname: redis
    ports:
          - 6379:6379